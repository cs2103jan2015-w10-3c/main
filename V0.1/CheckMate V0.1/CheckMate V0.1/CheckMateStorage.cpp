#include "checkmate.h"

addItem (string description, string starttime, string endtime, string category){

	item.desc=description;
	item.stime=starttime;
	item.etime=endtime;
	item.cat=category;

	v.pushback(item);
	
}